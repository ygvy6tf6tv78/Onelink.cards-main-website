"use client";

import Link from "next/link";
import { useState } from "react";
import { QuickBillShell } from "@/components/quickbill/quickbill-ui";

export default function CreateBillPage() {
  const [quantity, setQuantity] = useState(1); const [rate, setRate] = useState(12500); const [discount, setDiscount] = useState(0);
  const total = quantity * rate - discount;
  return <QuickBillShell><main className="mx-auto max-w-6xl px-4 py-8 sm:px-7 sm:py-10"><Link href="/quickbill" className="inline-flex items-center gap-2 text-sm font-bold text-slate-500 hover:text-sky-600">← Back to dashboard</Link><div className="mt-5"><p className="text-sm font-bold text-sky-600">New bill</p><h1 className="mt-1 text-3xl font-extrabold tracking-[-.045em]">Create a bill in seconds.</h1><p className="mt-2 text-sm font-medium text-slate-500">Add the essentials now. You can always edit details later.</p></div><div className="mt-8 grid items-start gap-6 lg:grid-cols-[1fr_340px]"><form className="space-y-5"><FormCard title="Customer details" subtitle="Who is this bill for?"><div className="grid gap-4 sm:grid-cols-2"><Field label="Customer name" placeholder="e.g. Aarav Mehta" /><Field label="Phone number" placeholder="+91 98765 43210" type="tel" /></div></FormCard><FormCard title="Bill details" subtitle="A few lines are all you need."><div className="grid gap-4 sm:grid-cols-3"><Field label="Bill number" placeholder="QB-0001" /><Field label="Bill date" value="26 Jul 2026" /><Field label="Due date (optional)" placeholder="Select date" /></div><div className="mt-6 overflow-hidden rounded-xl border border-slate-200"><div className="grid grid-cols-[1fr_58px_82px] gap-2 bg-slate-50 px-3 py-2.5 text-[10px] font-bold uppercase tracking-wider text-slate-400 sm:grid-cols-[1fr_84px_100px_100px]"><span>Item / service</span><span>Qty</span><span>Rate</span><span className="hidden text-right sm:block">Amount</span></div><div className="grid grid-cols-[1fr_58px_82px] items-center gap-2 px-3 py-3 sm:grid-cols-[1fr_84px_100px_100px]"><input defaultValue="Brand identity consultation" className="min-w-0 bg-transparent text-sm font-semibold outline-none" /><input value={quantity} type="number" min="1" onChange={(event) => setQuantity(Number(event.target.value))} className="min-w-0 bg-transparent text-sm font-semibold outline-none" /><input value={rate} type="number" min="0" onChange={(event) => setRate(Number(event.target.value))} className="min-w-0 bg-transparent text-sm font-semibold outline-none" /><span className="hidden text-right text-sm font-extrabold sm:block">₹{quantity * rate}</span></div></div><button type="button" className="mt-3 text-sm font-bold text-sky-600">+ Add another item</button><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field label="Discount (optional)" type="number" value={String(discount)} onChange={(value) => setDiscount(Number(value))} /><Field label="Notes (optional)" placeholder="A short note for customer" /></div></FormCard></form><aside className="sticky top-20 rounded-2xl border border-slate-200 bg-white p-5 shadow-[0_22px_50px_-35px_rgba(15,23,42,.35)]"><p className="text-sm font-extrabold">Bill summary</p><div className="mt-5 space-y-3 border-b border-slate-100 pb-5 text-sm"><div className="flex justify-between text-slate-500"><span>Subtotal</span><span>₹{quantity * rate}</span></div><div className="flex justify-between text-slate-500"><span>Discount</span><span>− ₹{discount}</span></div><div className="flex justify-between text-base font-extrabold text-slate-900"><span>Total amount</span><span>₹{total}</span></div></div><div className="mt-5 rounded-xl bg-amber-50 p-3"><p className="text-[11px] font-bold uppercase tracking-wider text-amber-700">Payment status</p><p className="mt-1 text-sm font-extrabold text-amber-700">Pending</p></div><Link href="/quickbill/success" className="qb-primary mt-5 w-full">Generate bill <span>→</span></Link><p className="mt-3 text-center text-[11px] font-medium text-slate-400">Your customer gets a secure bill link instantly.</p></aside></div></main></QuickBillShell>;
}
function FormCard({ title, subtitle, children }: { title: string; subtitle: string; children: React.ReactNode }) { return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-[0_22px_50px_-38px_rgba(15,23,42,.3)] sm:p-6"><h2 className="text-lg font-extrabold tracking-[-.03em]">{title}</h2><p className="mt-1 text-xs font-medium text-slate-400">{subtitle}</p><div className="mt-5">{children}</div></section>; }
function Field({ label, placeholder, type = "text", value, onChange }: { label: string; placeholder?: string; type?: string; value?: string; onChange?: (value: string) => void }) { return <label className="block text-xs font-bold text-slate-600">{label}<input type={type} value={value} onChange={(event) => onChange?.(event.target.value)} placeholder={placeholder} className="mt-2 block w-full rounded-xl border border-slate-200 bg-slate-50/50 px-3.5 py-3 text-sm font-medium text-slate-800 outline-none placeholder:text-slate-400 focus:border-sky-300 focus:bg-white" /></label>; }
